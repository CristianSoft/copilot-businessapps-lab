# Publishing

Use `pnpm validate-manifest` before publishing the plugin package.

The publishing process typically includes:

1. Validate the manifest and assets.
2. Build and package the plugin.
3. Publish the ZIP package to your internal marketplace or deployment pipeline.

This project includes `.github/workflows/release.yml` for GitHub Actions release automation.
