# Authentication

This template includes placeholders for authentication and secure API integration.

- Use `OPENAI_API_KEY` in `apps/mcp-server/.env` for OpenAI integration.
- Replace placeholder endpoints with your secured platform API.
- Add OAuth, managed identity, or federated credentials as required for your enterprise environment.

For production, ensure secrets are stored in a secure vault and not committed to source control.
